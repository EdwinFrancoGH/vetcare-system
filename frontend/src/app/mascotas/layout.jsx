export default function MascotasLayout({ children }) {//solo muestra la pagina-Sirve para agregar elementos comunes a todas las páginas de mascotas
    return (
        <>
            {children}
        </>
    );
}